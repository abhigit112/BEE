import React from "react";

const Redirect = () => {
  console.log("Board component rendered"); // Add this line for debugging

  return (
    <h1>Hello, this is the Dashboard for invoice sending</h1>
  );
};

export default Redirect;
